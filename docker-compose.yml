version: '3.8'
services:
  guacd:
    image: guacamole/guacd:1.5.5
    restart: unless-stopped
    ports:
      - "4822:4822"

  guacamole:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      GUACAMOLE_HOME: /guacamole
    depends_on:
      - guacd
